---
phase: 07-requirements-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CAP-06
  - CAP-08
  - TEST-03
  - CWS-01
  - CWS-05
must_haves:
  truths:
    - "CAP-06 text says no countdown timer, matching implementation and CLAUDE.md"
    - "CAP-08 text says web tab only, no native helper reference"
    - "TEST-03 text references web tab mode only, no native helper mode"
    - "CWS-01 text does not reference nativeMessaging permission"
    - "CWS-05 text does not reference native helper"
    - "ROADMAP plan checkboxes match actual completion state for all phases"
    - "ROADMAP progress table shows correct plan counts for all phases"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected requirement text for CAP-06, CAP-08, TEST-03, CWS-01, CWS-05"
      contains: "Web tab CAPTCHA solving"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate checkboxes and progress for all phases"
      contains: "[x]"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "Requirement IDs match audit gap findings"
      pattern: "CAP-06|CAP-08|TEST-03|CWS-01|CWS-05"
---

<objective>
Fix obsolete requirement text and sync ROADMAP state to reality, closing all documentation gaps identified by the v1.0 milestone audit.

Purpose: The milestone audit found 3 unsatisfied requirements (CAP-06, CAP-08, TEST-03) due to obsolete text referencing the abandoned native helper or a non-existent countdown timer. Additionally, CWS-01 and CWS-05 text references nativeMessaging/native helper which were removed. ROADMAP plan checkboxes for phases 3, 5, 6 are unchecked despite completion.

Output: Corrected REQUIREMENTS.md and ROADMAP.md matching actual implementation state.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix obsolete requirement text in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read .planning/REQUIREMENTS.md and make these exact text corrections:

1. **CAP-06** (line ~39): Change from:
   `5-minute timeout countdown displayed on CAPTCHA page; auto-skips on expiry`
   To:
   `No countdown timer on CAPTCHA page (old MV2 never had one); tab close triggers skip`
   Also change the checkbox from `[ ]` to `[x]` since the implementation correctly has no countdown (matches CLAUDE.md and Phase 4 success criteria #6).

2. **CAP-08** (line ~41): Change from:
   `Dual-mode: uses native helper when installed, falls back to web tab when not`
   To:
   `Web tab CAPTCHA solving only — opens browser tab on target domain via MyJD API (native helper abandoned)`
   Also change the checkbox from `[ ]` to `[x]` since web tab mode is fully implemented.

3. **TEST-03** (line ~49): Change from:
   `Both web tab and native helper modes tested with reCAPTCHA v2 and hCaptcha`
   To:
   `Web tab mode tested with reCAPTCHA v2 and hCaptcha (native helper mode removed)`
   Keep checkbox as `[ ]` — this requirement is addressed by Phase 8 verification, not this text fix.

4. **CWS-01** (line ~58): Change from:
   `Every permission has written justification (\`<all_urls>\`, \`nativeMessaging\`, \`tabs\`, \`scripting\`, \`offscreen\`, \`declarativeNetRequest\`)`
   To:
   `Every permission has written justification (\`<all_urls>\`, \`tabs\`, \`scripting\`, \`offscreen\`, \`declarativeNetRequest\`)`
   (Remove nativeMessaging from the list since that permission was removed from manifest.json)

5. **CWS-05** (line ~62): Change from:
   `Extension description accurately reflects MV3 features including native helper requirement`
   To:
   `Extension description accurately reflects MV3 features (web tab CAPTCHA, no native helper)`

6. **Traceability table**: Update CAP-06 and CAP-08 rows:
   - CAP-06: Change Phase from "Phase 7" to "Phase 4, 7" and Status from "Pending" to "Complete"
   - CAP-08: Change Phase from "Phase 7" to "Phase 4, 7" and Status from "Pending" to "Complete"
   - TEST-03: Keep Phase as "Phase 8" and Status as "Pending" (text corrected here, verification in Phase 8)

7. **Last updated**: Change the last updated line to `*Last updated: 2026-03-08 after Phase 7 requirement text cleanup*`
  </action>
  <verify>
    <automated>grep -c "native helper\|nativeMessaging\|countdown.*displayed\|Dual-mode" .planning/REQUIREMENTS.md | grep -q "^0$" && echo "PASS: no obsolete references" || echo "FAIL: obsolete references remain"</automated>
  </verify>
  <done>CAP-06 says no countdown, CAP-08 says web tab only, TEST-03 says web tab mode only, CWS-01 does not list nativeMessaging, CWS-05 does not reference native helper. All checked boxes match implementation state.</done>
</task>

<task type="auto">
  <name>Task 2: Sync ROADMAP checkboxes and progress to reality</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read .planning/ROADMAP.md and make these corrections:

1. **Phase 3 plan list** (~line 65): Change:
   `- [ ] 03-01-PLAN.md — StorageService setting key...`
   To:
   `- [x] 03-01-PLAN.md — StorageService setting key...`

2. **Phase 5 plan list** (~lines 96-97): Change:
   `- [ ] 05-01-PLAN.md — Localhost flow code review...`
   `- [ ] 05-02-PLAN.md — Live E2E CAPTCHA testing...`
   To:
   `- [x] 05-01-PLAN.md — Localhost flow code review...`
   `- [x] 05-02-PLAN.md — Live E2E CAPTCHA testing...`

3. **Phase 6 plan list** (~lines 112-113): Change:
   `- [ ] 06-01-PLAN.md — Manifest cleanup...`
   `- [ ] 06-02-PLAN.md — CSP runtime verification...`
   To:
   `- [x] 06-01-PLAN.md — Manifest cleanup...`
   `- [x] 06-02-PLAN.md — CSP runtime verification...`

4. **Phase 3 plans count** (~line 62): Change:
   `**Plans:** 1 plan`
   To:
   `**Plans:** 1/1 Complete`

5. **Phase 5 plans count** (~line 93): Change:
   `**Plans:** 2 plans`
   To:
   `**Plans:** 2/2 Complete`

6. **Phase 6 plans count** (~line 109): Change:
   `**Plans:** 2 plans`
   To:
   `**Plans:** 2/2 Complete`

7. **Phase 7 in progress table** (~line 160): Update from `0/0 | Pending` to `1/1 | In Progress` (this plan exists now).

8. **Phase 7 plans section** (~line 127): Change:
   `**Plans:** 0 plans`
   To:
   `**Plans:** 1 plan`
   And add plan list:
   ```
   Plans:
   - [ ] 07-01-PLAN.md — Fix obsolete requirement text + sync ROADMAP state
   ```

9. **Last updated line**: Change to `*Last updated: 2026-03-08 (Phase 7 planning)*`
  </action>
  <verify>
    <automated>grep -c "\- \[ \] 03-01\|\- \[ \] 05-01\|\- \[ \] 05-02\|\- \[ \] 06-01\|\- \[ \] 06-02" .planning/ROADMAP.md | grep -q "^0$" && echo "PASS: all completed plan checkboxes are checked" || echo "FAIL: unchecked completed plans remain"</automated>
  </verify>
  <done>Phase 3/5/6 plan checkboxes show [x]. Plan counts show correct N/N format. Progress table reflects Phase 7 planning state. No stale checkbox or count data remains.</done>
</task>

</tasks>

<verification>
1. `grep "CAP-06" .planning/REQUIREMENTS.md` shows "No countdown timer" text
2. `grep "CAP-08" .planning/REQUIREMENTS.md` shows "Web tab CAPTCHA solving only" text
3. `grep "TEST-03" .planning/REQUIREMENTS.md` shows "Web tab mode tested" text
4. `grep "CWS-01" .planning/REQUIREMENTS.md` does NOT contain "nativeMessaging"
5. `grep "CWS-05" .planning/REQUIREMENTS.md` does NOT contain "native helper requirement"
6. All Phase 3/5/6 plan checkboxes in ROADMAP.md show `[x]`
</verification>

<success_criteria>
- Zero references to "native helper" in requirement text for CAP-08, TEST-03, CWS-05
- Zero references to "nativeMessaging" in CWS-01 text
- Zero references to "countdown displayed" in CAP-06 text
- All completed phase plan checkboxes show [x] in ROADMAP
- Phase 7 shows 1 plan in ROADMAP
</success_criteria>

<output>
After completion, create `.planning/phases/07-requirements-docs-cleanup/07-01-SUMMARY.md`
</output>
