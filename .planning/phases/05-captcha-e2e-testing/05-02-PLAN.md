---
phase: 05-captcha-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md
autonomous: false
requirements: [TEST-01, TEST-03]

must_haves:
  truths:
    - "MYJD remote CAPTCHA flow works end-to-end: trigger -> tab opens -> widget renders -> user solves -> token submits -> download starts"
    - "Tab close sends skip signal to JDownloader"
    - "At least reCAPTCHA v2 or hCaptcha confirmed working through live test"
    - "JDownloader receives the solved token and proceeds with download"
  artifacts:
    - path: ".planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md"
      provides: "Live E2E test results with pass/fail for each test"
      contains: "PASS"
  key_links:
    - from: "contentscripts/myjdCaptchaSolver.js"
      to: "chrome.storage.session (myjd_captcha_job)"
      via: "Session storage read on content script activation"
      pattern: "myjd_captcha_job"
    - from: "background.js (captcha-solved handler)"
      to: "my.jdownloader.org tabs (webinterfaceEnhancer.js)"
      via: "chrome.tabs.sendMessage to relay token"
      pattern: "captcha-solved.*MYJD"
---

<objective>
Execute live E2E CAPTCHA testing using the test script from Plan 01. Trigger real CAPTCHAs via file hosters, verify the full MYJD remote flow, and document results.

Purpose: This is the first live test of the CAPTCHA implementation built in Phase 4. The goal is validated confidence that the MYJD remote CAPTCHA flow works identically to the old MV2 extension. If bugs are found, debug and fix inline.

Output: Test results document with pass/fail for each test, CAPTCHA types observed, and any fixes applied.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-captcha-e2e-testing/05-CONTEXT.md
@.planning/phases/05-captcha-e2e-testing/05-RESEARCH.md
@.planning/phases/05-captcha-e2e-testing/05-E2E-TEST-SCRIPT.md
@.planning/phases/05-captcha-e2e-testing/05-01-SUMMARY.md

<!-- Key source files (may need to debug/fix during testing) -->
@contentscripts/myjdCaptchaSolver.js
@contentscripts/webinterfaceEnhancer.js
@scripts/services/Rc2Service.js
@background.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute MYJD remote CAPTCHA E2E test</name>
  <files>.planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md</files>
  <action>
Execute the E2E test script from 05-E2E-TEST-SCRIPT.md using browser MCP controls. Claude drives the testing; user only solves CAPTCHAs and confirms downloads.

**Step 1: Prerequisites verification**
- Use browser MCP to verify extension is loaded and connected
- Verify my.jdownloader.org is open in a tab
- Confirm JDownloader is running and connected (check MyJD web interface)

**Step 2: Find CAPTCHA-gated download links**
- Research file hosters that trigger CAPTCHAs for free users (try Rapidgator, Nitroflare, Turbobit)
- Skip pixeldrain (user is premium)
- Find or create a download link that will trigger a CAPTCHA when JDownloader processes it
- May need to search for existing public download links on these hosters

**Step 3: Execute Test 1 — Full E2E flow**
- Add the CAPTCHA-gated link to JDownloader
- Monitor for CAPTCHA tab to open in the browser
- When CAPTCHA tab appears, verify: correct domain, #rc2jdt hash, widget renders
- Prompt user to solve the CAPTCHA
- After solving, verify: tab auto-closes, JDownloader starts download
- Document pass/fail at each checkpoint

**Step 4: Execute Test 2 — Tab close = skip**
- If possible, trigger another CAPTCHA
- When tab opens, close it manually (do NOT solve)
- Verify JDownloader marks CAPTCHA as skipped

**Step 5: Verify extension state**
- Check chrome.storage.session for leftover CAPTCHA job data
- Check CSP rules cleanup via declarativeNetRequest
- Check service worker console for errors

**Step 6: Document CAPTCHA types observed (TEST-03)**
- Record which CAPTCHA type(s) were encountered (reCAPTCHA v2, v3, hCaptcha)
- Note the file hoster and CAPTCHA provider for each test

**On failure at any step:**
- Capture all diagnostic info (service worker logs, storage state, CSP rules, network errors)
- Identify root cause
- Fix the code inline
- Re-test the fixed flow
- Document the fix in test results

Create `.planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md` with:
- Test environment details (browser version, extension state, JD status)
- Per-test results with steps taken, expected vs actual, PASS/FAIL
- CAPTCHA types encountered
- Any bugs found and fixes applied
- Screenshots or console output as evidence
  </action>
  <verify>
    <automated>test -f ".planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md" && grep -c "PASS\|FAIL" ".planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md"</automated>
  </verify>
  <done>05-TEST-RESULTS.md exists with documented results for all executed tests. At least the MYJD remote flow test has a result (PASS or FAIL with documented fix). CAPTCHA type(s) encountered are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms CAPTCHA E2E results</name>
  <files>.planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md</files>
  <action>
Present the completed 05-TEST-RESULTS.md to the user for final confirmation. The user verifies:
1. JDownloader actually received the solved token and started (or attempted) the download
2. The CAPTCHA tab appeared and the widget rendered correctly
3. Any fixes applied during testing are acceptable
4. TEST-01 (E2E flow documented), TEST-02 (localhost code review from Plan 01), and TEST-03 (CAPTCHA types tested) are all satisfied
  </action>
  <verify>
    <automated>grep -c "PASS" ".planning/phases/05-captcha-e2e-testing/05-TEST-RESULTS.md"</automated>
  </verify>
  <done>User has reviewed 05-TEST-RESULTS.md and confirmed the CAPTCHA E2E testing results are acceptable. All three TEST requirements are satisfied.</done>
</task>

</tasks>

<verification>
- 05-TEST-RESULTS.md exists with per-test pass/fail results
- MYJD remote CAPTCHA flow confirmed working (or bugs found and fixed)
- CAPTCHA type coverage documented (reCAPTCHA v2 and/or hCaptcha)
- Tab close skip behavior verified
- Extension state clean after testing (no leftover storage, CSP rules)
- User has confirmed results
- `npx jest` still passes (if any code fixes were applied)
</verification>

<success_criteria>
- TEST-01: Full CAPTCHA flow documented with pass/fail at each step
- TEST-03: At least one CAPTCHA type confirmed working through live test; localhost flow covered by Plan 01 code review
- User confirms JDownloader received solved token and processed the download
- Any bugs found during testing are fixed and re-verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-captcha-e2e-testing/05-02-SUMMARY.md`
</output>
