---
phase: 06-mv3-compliance-audit
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md
autonomous: false
requirements: [CWS-04]

must_haves:
  truths:
    - "Extension loads cleanly with zero console CSP warnings on all pages"
    - "CSP test results are recorded in the compliance report"
  artifacts:
    - path: ".planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md"
      provides: "Updated compliance report with runtime CSP test results"
      contains: "CSP Compliance"
  key_links:
    - from: "manifest.json"
      to: "06-COMPLIANCE-REPORT.md"
      via: "Runtime CSP test confirms manifest compliance"
      pattern: "PASS|No CSP"
---

<objective>
Verify zero CSP violations at runtime by loading the cleaned-up extension in Chrome and checking DevTools consoles on all extension pages.

Purpose: CWS-04 requires confirmed absence of CSP violations. This cannot be automated -- it requires loading the actual extension in Chrome and inspecting each page's console.
Output: Updated compliance report with runtime CSP test results.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mv3-compliance-audit/06-CONTEXT.md
@.planning/phases/06-mv3-compliance-audit/06-RESEARCH.md
@.planning/phases/06-mv3-compliance-audit/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Runtime CSP verification on all extension pages</name>
  <what-built>Plan 01 removed nativeMessaging from manifest.json, cleaned web_accessible_resources, and removed CaptchaNativeService.js from popup.html. The compliance report documents expected CSP behavior. This checkpoint verifies zero CSP violations at runtime.</what-built>
  <how-to-verify>
1. Go to `chrome://extensions/` and reload the extension (click the refresh icon on "MyJDownloader MV3")
2. Verify the extension loads without errors (no "Errors" button appearing)

3. **Test popup.html:**
   - Right-click the extension icon > "Inspect popup" to open DevTools
   - Click the extension icon to open the popup
   - In DevTools Console, filter for "CSP" or "Refused" errors
   - Expected: No CSP errors. AngularJS uses ng-csp mode.

4. **Test background.js (service worker):**
   - On `chrome://extensions/`, click "Inspect views: service worker" for MyJDownloader
   - Check Console for any CSP-related errors
   - Expected: No CSP errors. Service worker has no DOM operations.

5. **Test toolbar.html:**
   - Right-click any link on any webpage
   - Select "Download with JDownloader" from context menu
   - The toolbar iframe should appear
   - Right-click inside the toolbar iframe > Inspect to open DevTools
   - Check Console for CSP errors
   - Expected: No CSP errors. ng-csp is present.

6. **Test offscreen.html:**
   - While popup is open and logged in, click any device to trigger an API call
   - Go to `chrome://extensions/`, look for "Inspect views: offscreen.html"
   - Check Console for CSP errors
   - Expected: No CSP errors. No AngularJS loaded; jQuery/CryptoJS/RequireJS all external.

Report results: For each page, state "PASS - no CSP errors" or describe any errors found.
  </how-to-verify>
  <resume-signal>Type "approved" with results for each page, or describe any CSP errors found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record CSP test results in compliance report</name>
  <files>.planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md</files>
  <action>
Update the "CSP Compliance (CWS-04)" section of 06-COMPLIANCE-REPORT.md to include the actual runtime test results from the checkpoint.

For each page tested (popup.html, background.js, toolbar.html, offscreen.html, loginNeeded.html), add a results row:

```markdown
| Page | CSP Errors | Status |
|------|-----------|--------|
| popup.html | None | PASS |
| background.js (service worker) | None | PASS |
| toolbar.html | None | PASS |
| offscreen.html | None | PASS |
| loginNeeded.html | N/A (no scripts) | PASS |
```

If any errors were found during the checkpoint, document them with the exact error message and add an ACTION NEEDED recommendation.

This converts the CSP section from "expected behavior" to "verified behavior."
  </action>
  <verify>
    <automated>grep -c "PASS" ".planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md" | awk '{if ($1 >= 5) print "PASS: CSP results recorded"; else print "CHECK: fewer than expected PASS entries"}'</automated>
  </verify>
  <done>
- CSP Compliance section in 06-COMPLIANCE-REPORT.md contains runtime test results
- Each extension page has a documented PASS or documented error
- Report reflects actual verified behavior, not just expected behavior
  </done>
</task>

</tasks>

<verification>
1. 06-COMPLIANCE-REPORT.md CSP section contains runtime test results table
2. All extension pages show PASS (or any errors are documented with remediation)
3. Extension loads and functions correctly after manifest cleanup
</verification>

<success_criteria>
- User has verified zero CSP errors across all extension pages
- Compliance report CSP section updated with actual test results
- CWS-04 requirement satisfied with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/06-mv3-compliance-audit/06-02-SUMMARY.md`
</output>
