---
phase: 06-mv3-compliance-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - popup.html
  - .planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md
  - README.md
autonomous: true
requirements: [CWS-01, CWS-02, CWS-03, CWS-05, CWS-06, CWS-07]

must_haves:
  truths:
    - "nativeMessaging permission is removed from manifest.json"
    - "CaptchaNativeService.js script tag is removed from popup.html"
    - "captcha-helper test files are removed from web_accessible_resources"
    - "Every declared permission has a written justification in the compliance report"
    - "All eval/Function() usage in vendor files is documented with dead-code analysis"
    - "postMessage wildcard in webinterfaceEnhancer.js is documented as a finding"
    - "Handoff items (privacy policy, store listing, screenshots) are listed for JD developers"
    - "README contains MV2-to-MV3 migration summary table"
  artifacts:
    - path: "manifest.json"
      provides: "Clean MV3 manifest without nativeMessaging"
      contains: "\"permissions\""
    - path: "popup.html"
      provides: "Popup without CaptchaNativeService.js script tag"
    - path: ".planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md"
      provides: "Professional compliance report for JD developers"
      min_lines: 100
    - path: "README.md"
      provides: "MV2 to MV3 migration table"
      contains: "MV2"
  key_links:
    - from: "manifest.json"
      to: "06-COMPLIANCE-REPORT.md"
      via: "Permission list matches audit"
      pattern: "permissions.*tabs.*storage.*declarativeNetRequest"
    - from: "06-COMPLIANCE-REPORT.md"
      to: "vendor/js/require.js"
      via: "eval audit cites specific lines"
      pattern: "req\\.exec|line 2140"
    - from: "06-COMPLIANCE-REPORT.md"
      to: "contentscripts/webinterfaceEnhancer.js"
      via: "postMessage finding cites specific line"
      pattern: "postMessage.*\\*|line 54"
---

<objective>
Remove abandoned permissions/code from the manifest, produce a professional MV3 compliance report for JDownloader developers, and add a migration summary table to the README.

Purpose: Prepare the extension for Chrome Web Store submission by cleaning up dead permissions and documenting all compliance findings so JD developers can act on them without ambiguity.
Output: Clean manifest.json, compliance report (06-COMPLIANCE-REPORT.md), updated README.md with migration table.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mv3-compliance-audit/06-CONTEXT.md
@.planning/phases/06-mv3-compliance-audit/06-RESEARCH.md

Source files to audit:
@manifest.json
@popup.html
@contentscripts/webinterfaceEnhancer.js
@vendor/js/require.js (line ~2140 for req.exec)
@vendor/js/angular.js (lines ~1269-1292 for ng-csp, line ~16548 for ASTCompiler)
@vendor/js/rx.all.js (line ~21 for global detection, lines ~1303/1341 for postMessage)
@offscreen.html
@toolbar.html
@loginNeeded.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest and popup cleanup</name>
  <files>manifest.json, popup.html</files>
  <action>
1. Edit manifest.json:
   - Remove "nativeMessaging" from the "permissions" array (line 74). The native helper is abandoned and unused.
   - Remove "captcha-helper/test-native-messaging.html" and "captcha-helper/test-native-messaging.js" from the "web_accessible_resources" resources array (lines 80-81). These are legacy test files for the abandoned native helper.
   - Keep all other permissions unchanged (tabs, storage, declarativeNetRequest, contextMenus, scripting, alarms, offscreen).
   - Keep all host_permissions unchanged (all_urls, localhost:9666).

2. Edit popup.html:
   - Remove the script tag loading CaptchaNativeService.js (line 43: `<script src="scripts/services/CaptchaNativeService.js"></script>`). The service is registered in Angular DI but never injected anywhere, and with nativeMessaging removed it is non-functional.

3. Do NOT delete CaptchaNativeService.js file from disk (kept per prior decision, just unloaded).
4. Do NOT modify any other files.
  </action>
  <verify>
    <automated>npx jest -x</automated>
  </verify>
  <done>
- manifest.json permissions array contains exactly: tabs, storage, declarativeNetRequest, contextMenus, scripting, alarms, offscreen (no nativeMessaging)
- manifest.json web_accessible_resources contains only: toolbar.html, autograbber-indicator.html (no captcha-helper files)
- popup.html has no script tag for CaptchaNativeService.js
- All 216 existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MV3 compliance report</name>
  <files>.planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md</files>
  <action>
Create a professional compliance report at `.planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md` structured for JDownloader developers. This is the primary deliverable of Phase 6 -- it must be thorough, cite specific file:line evidence, and categorize findings as PASS/FINDING/ACTION NEEDED.

Use the research from 06-RESEARCH.md as the source of all findings. Structure the report with these sections:

**1. Executive Summary**
- Brief overview: Extension is architecturally MV3 compliant. One permission removed (nativeMessaging). Vendor files contain restricted constructs that are dead code at runtime. One postMessage wildcard documented.
- Overall assessment: READY FOR SUBMISSION with noted considerations.

**2. Permission Audit (CWS-01)**
Create a table with columns: Permission | Justification | Used By (file references)
Include all 7 remaining permissions and 3 host_permissions:
- `tabs` — CAPTCHA tab management, toolbar injection, tab close detection (background.js, Rc2Service.js)
- `storage` — Session/settings persistence, CAPTCHA job transfer, request queue (background.js, offscreen.js, content scripts)
- `declarativeNetRequest` — CSP header stripping on CAPTCHA tabs, CNL localhost rules (background.js)
- `contextMenus` — "Download with JDownloader" right-click menu (background.js)
- `scripting` — MAIN world CAPTCHA execution for v3/invisible CAPTCHAs, toolbar injection (background.js)
- `alarms` — Service worker keep-alive timer (4-min interval) (background.js)
- `offscreen` — Offscreen document for jdapi operations needing DOM/localStorage (background.js, offscreen.js)
- `<all_urls>` (host) — Content scripts on all pages for CNL interception, toolbar injection, CAPTCHA solving on arbitrary domains
- `http://127.0.0.1:9666/*` (host) — JDownloader local API endpoint
- `http://localhost:9666/*` (host) — JDownloader local API endpoint (alias)

Include a "Removed Permissions" subsection:
- ~~`nativeMessaging`~~ — REMOVED. Native helper (captcha-helper/) is abandoned. CaptchaNativeService.js unloaded from popup.html.

**3. Code Safety Audit (CWS-03)**
Document each restricted code construct found in vendor files, using the format from 06-RESEARCH.md:

For each entry include: File, Line, Code description, Reachable?, Risk, Recommendation.

Cover these specific items:
- vendor/js/require.js line 2140: req.exec text-execution — dead code (only for loader plugins)
- vendor/js/angular.js line 1292: CSP detection probe — skipped by ng-csp attribute
- vendor/js/angular.js line 16548: ASTCompiler — bypassed by ng-csp (ASTInterpreter used instead)
- vendor/js/rx.all.js line 21: Global detection fallback — short-circuited in browser contexts
- vendor/js/rx.all.js line 1303: postMessage scheduling test — internal feature detection

Include the AngularJS ng-csp analysis explaining how popup.html and toolbar.html both have ng-csp on the body tag, which prevents AngularJS from ever using dynamic code generation.

Include the offscreen.html analysis: no AngularJS loaded, so ng-csp is not needed.

Add a "CWS Review Consideration" note: Chrome's static analysis during CWS review may flag the presence of restricted constructs in vendor files even though they are dead code at runtime. If this causes rejection, the recommendation is to either (a) add explanatory comments in vendor files, (b) pursue RequireJS removal (MOD-01), or (c) contact Chrome Web Store support with dead-code evidence.

**4. CSP Compliance (CWS-04)**
Document expected CSP behavior:
- MV3 default for extension pages: `script-src 'self'; object-src 'self'`
- popup.html: ng-csp prevents AngularJS from violating CSP. All scripts loaded as external files.
- toolbar.html: ng-csp present. All scripts external.
- offscreen.html: No AngularJS. jQuery, CryptoJS, RequireJS loaded externally.
- loginNeeded.html: Has inline `<style>` (CSS, not JS) — allowed under MV3 CSP which restricts script-src, not style-src.
- background.js: Service worker, no DOM, no script loading.
- Note: Runtime verification should be performed by loading the extension and checking DevTools console on each page for CSP error messages.

**5. postMessage Security (CWS-07)**
Document the finding from 06-RESEARCH.md:
- webinterfaceEnhancer.js line 54: `window.postMessage(msg, "*")` — wildcard origin
  - Context: Content script only runs on `*://my.jdownloader.org/*`, so wildcard is functionally scoped
  - Risk: LOW — any listener on the page can intercept the message
  - Recommended fix: Replace `"*"` with `"https://my.jdownloader.org"`
  - Note: Line 18 `window.parent.postMessage` correctly uses specific origin
  - Decision: Left unchanged per project decision — JD developers to evaluate

Document the rx.all.js internal postMessage usage (lines 1303/1341) as SAFE — internal scheduling mechanism with unique prefix, not cross-origin communication.

**6. Additional Findings**
- Dead code in webinterfaceEnhancer.js lines 56-64: unreachable captcha-done branch (duplicate condition). Recommend removal.
- autograbber-indicator.html: Has a script tag pointing to a directory (`/contentscripts/`), not a file. Non-functional but harmless. Recommend cleanup.
- CaptchaNativeService.js: File kept on disk but unloaded from popup.html. Can be deleted entirely if desired.

**7. Handoff Items for JD Developers**
Items this audit intentionally does not address:
- CWS-02: Privacy policy — Must be created and hosted, covering credential handling (MyJD login), URL data (links sent to JDownloader), storage usage (chrome.storage.local/session). Link in CWS listing.
- CWS-05: Extension description — Update to reflect MV3 features. Remove any references to native helper.
- CWS-06: Screenshots — At least 2 screenshots showing core features (login/device selection, context menu/toolbar).
- Consider deleting CaptchaNativeService.js and captcha-helper/ directory entirely.
- Consider fixing the postMessage wildcard per the recommendation above.

Use a professional, factual tone throughout. Cite specific file:line references for every claim. This document represents the extension's compliance posture to the JDownloader development team.
  </action>
  <verify>
    <automated>test -f ".planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md" && wc -l ".planning/phases/06-mv3-compliance-audit/06-COMPLIANCE-REPORT.md" | awk '{if ($1 >= 100) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>
- 06-COMPLIANCE-REPORT.md exists with 100+ lines
- Report covers all 7 CWS requirements (CWS-01 through CWS-07)
- Every permission has a written justification with file references
- Every eval/Function() location in vendor files is documented with reachability analysis
- postMessage wildcard in webinterfaceEnhancer.js is documented with recommended fix
- Handoff items clearly listed for JD developers
- All findings categorized as PASS/FINDING/ACTION NEEDED
  </done>
</task>

<task type="auto">
  <name>Task 3: Add MV2-to-MV3 migration table to README</name>
  <files>README.md</files>
  <action>
Add a new "MV2 to MV3 Migration" section to README.md. Place it after the "Development" section and before the "License" section.

Use this content (per CONTEXT.md -- non-technical, just the gist):

```markdown
## MV2 to MV3 Migration

This extension was converted from Manifest V2 to Manifest V3. Here is a summary of what changed:

| MV2 Approach | MV3 Replacement | Why |
|-------------|-----------------|-----|
| Background page (persistent) | Service worker (event-driven) | MV3 requires non-persistent background contexts |
| `chrome.tabs.executeScript()` | `chrome.scripting.executeScript()` | New API with explicit world targeting (MAIN/ISOLATED) |
| Inline script injection for CAPTCHAs | External script elements + content scripts | MV3 CSP prohibits inline script execution |
| `chrome.browserAction` | `chrome.action` | API renamed in MV3 |
| `localStorage` in background page | `chrome.storage.session` + offscreen document | Service workers have no DOM or localStorage access |
| Unrestricted CSP | Default `script-src 'self'` + `ng-csp` | MV3 enforces strict Content Security Policy |
```

Do NOT modify any other section of the README. Keep all existing content intact.
  </action>
  <verify>
    <automated>grep -c "MV2 to MV3 Migration" README.md | awk '{if ($1 >= 1) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>
- README.md contains "MV2 to MV3 Migration" section
- Table has 6 rows covering background, scripting, CAPTCHA, action API, storage, and CSP changes
- Section is placed between Development and License sections
- All existing README content is preserved
  </done>
</task>

</tasks>

<verification>
1. `npx jest` — all existing tests pass (regression check after manifest/popup changes)
2. `grep "nativeMessaging" manifest.json` — returns no matches
3. `grep "CaptchaNativeService" popup.html` — returns no matches
4. `grep "test-native-messaging" manifest.json` — returns no matches
5. 06-COMPLIANCE-REPORT.md exists and covers all 7 CWS requirements
6. README.md contains MV2-to-MV3 migration table
</verification>

<success_criteria>
- manifest.json has nativeMessaging removed, web_accessible_resources cleaned
- popup.html no longer loads CaptchaNativeService.js
- Professional compliance report created with permission audit, code safety audit, CSP analysis, postMessage findings, and handoff items
- README contains MV2-to-MV3 migration table
- All 216 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-mv3-compliance-audit/06-01-SUMMARY.md`
</output>
