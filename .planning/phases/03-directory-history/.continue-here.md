---
phase: 03-directory-history
task: 1
total_tasks: 1
status: bugfix_in_progress
last_updated: "2026-03-08T19:46:15.966Z"
---

<current_state>
Directory history feature was marked complete in Phase 3 but the dropdown is NOT actually working.
User confirmed: the saveto input is a plain text field with no dropdown suggestions appearing.
The OTHER optional fields (comment, packageName, archivepw, downloadpw) also use HTML5 `<datalist>`
and they DON'T show dropdowns either — they only save/restore via the "Restore last used values" button.
So the datalist approach may fundamentally not work in Chrome extension popup/iframe contexts.

A dropdown arrow button was added to the saveto field (committed but untested) but the user reports
it's STILL not working — meaning the underlying datalist mechanism doesn't show a dropdown.
</current_state>

<completed_work>

- Phase 3 original implementation: addToHistory with normalize/dedup/MRU/cap, clearSavetoHistory,
  directoryHistoryEnabled setting, datalist in template, CSS for clear button — ALL DONE
- Phase 9: Added DIRECTORY_HISTORY_ENABLED toggle checkbox to settings page — DONE
- This session: Added visual dropdown arrow button (.saveto-dropdown-btn) with showSavetoDropdown()
  function that focuses input to trigger datalist — DONE but NOT WORKING
- This session: Deep code analysis of save/restore paths — no bugs found in the data flow
- All 222 tests pass
</completed_work>

<remaining_work>

- ROOT CAUSE: HTML5 `<datalist>` likely doesn't render a visible dropdown in Chrome extension
  popup/toolbar iframe contexts. The datalist DATA is being saved/restored correctly (the "Restore
  last used values" button works, proving the storage layer is fine), but the browser's native
  datalist dropdown UI doesn't appear.

- SOLUTION OPTIONS (pick one):
  1. **Replace `<datalist>` with a custom AngularJS dropdown** — use ng-repeat to render a visible
     `<div>` dropdown below the input, with click-to-select. This is the most reliable approach.
  2. **Replace `<datalist>` with a `<select>` + `<input>` combo** — show a real `<select>` element
     that the user can pick from, with the text input for typing new values.
  3. **Use AngularJS UI Bootstrap typeahead** — but this adds a dependency.

- After fixing the dropdown UI, verify:
  - Dropdown shows previously saved directories when clicking the arrow or focusing the input
  - Selecting a dropdown entry populates the saveto field
  - New directories are added after successful Send
  - Clear button empties all entries
  - directoryHistoryEnabled toggle hides/shows the dropdown
  - Works in BOTH popup and toolbar iframe contexts
</remaining_work>

<decisions_made>

- HTML5 `<datalist>` was the original Phase 3 decision ("Native autocomplete; no extra JS/AngularJS
  needed") but it doesn't work in Chrome extension contexts — needs to be replaced
- The data layer (addToHistory, saveOptionsAndHistory, restoreOptionsAndHistory, StorageService) is
  correct and should NOT be changed — only the UI rendering needs replacement
- The "Restore last used values" button (autoFill) proves the storage/restore path works fine
</decisions_made>

<blockers>
- Could not get Chrome with remote debugging to load the extension for live testing. The
  `--load-extension` flag didn't work with the debug user-data-dir. Manual testing by user is needed.
- Browser MCP tools couldn't connect to user's normal Chrome profile (requires non-default data dir)
</blockers>

<context>
The key insight from the user: the OTHER optional fields (comment, packageName, etc.) also use
`<datalist>` and they DON'T show dropdowns either. They work purely through the save/restore
mechanism + the "Restore last used values" (autoFill) button. The user wants an ACTUAL VISIBLE
DROPDOWN for the saveto field — not just a datalist that may or may not trigger browser-native
autocomplete.

The fix needs to be a custom dropdown widget (likely a simple ng-repeat div positioned below the
input) that:
1. Shows all saved directory entries when the arrow is clicked
2. Allows clicking an entry to fill the input
3. Dismisses when clicking outside
4. Is visible and discoverable (not relying on browser-native datalist behavior)

The data layer is solid. Only the presentation needs changing.
</context>

<next_action>
1. Replace the `<datalist>` + dropdown arrow with a custom AngularJS dropdown:
   - Add `$scope.savetoDropdownOpen` boolean to AddLinksController
   - Toggle it from the arrow button click
   - Render a positioned `<div>` with `ng-repeat` over `history.saveto`
   - On entry click: set `$scope.selection.saveto` and close dropdown
   - Add click-outside-to-close behavior
   - Style with CSS (absolute positioning below the input, border, background, z-index)
2. Keep the `<datalist>` as a fallback (harmless if it doesn't render)
3. Test in both popup and toolbar contexts
</next_action>
