---
phase: 09-settings-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - options.html
  - options.js
  - scripts/__tests__/options.test.js
  - scripts/__tests__/background.test.js
autonomous: true
requirements:
  - SET-01
  - SET-02
must_haves:
  truths:
    - "background.js STORAGE_KEYS values match StorageService key strings exactly"
    - "background.js line 821 uses correct StorageService key strings (ADD_LINKS_DIALOG_ACTIVE, DEFAULT_PREFERRED_JD)"
    - "options.html and options.js are deleted from the project"
    - "No file in the codebase uses the wrong APP_KEY myjd_webextension_mv3"
  artifacts:
    - path: "background.js"
      provides: "Fixed STORAGE_KEYS and hardcoded key references"
      contains: "CLICKNLOAD_ACTIVE: 'CLICKNLOAD_ACTIVE'"
    - path: "scripts/__tests__/background.test.js"
      provides: "Storage key consistency test"
      contains: "STORAGE_KEYS"
    - path: "scripts/__tests__/options.test.js"
      provides: "Updated test verifying options files deleted"
      contains: "options.html"
  key_links:
    - from: "background.js"
      to: "StorageService.js"
      via: "matching chrome.storage.local key strings"
      pattern: "CLICKNLOAD_ACTIVE.*'CLICKNLOAD_ACTIVE'"
---

<objective>
Fix storage key mismatches between background.js and StorageService, delete the dead standalone options page, and update tests.

Purpose: background.js uses wrong storage key strings (settings_clicknload_active vs CLICKNLOAD_ACTIVE), so settings changes in the popup never propagate to the service worker. The standalone options page uses the wrong APP_KEY and duplicates functionality already in the AngularJS settings route.

Output: background.js with correct key strings, options.html/options.js deleted, tests updated.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-audit/09-RESEARCH.md
</context>

<interfaces>
<!-- Key constants the executor needs -->

From scripts/services/StorageService.js (source of truth for key strings):
```javascript
this.SETTINGS_CLICKNLOAD_ACTIVE = "CLICKNLOAD_ACTIVE";
this.SETTINGS_CONTEXT_MENU_SIMPLE = "CONTEXT_MENU_SIMPLE";
this.SETTINGS_DEFAULT_PREFERRED_JD = "DEFAULT_PREFERRED_JD";
this.SETTINGS_ADD_LINKS_DIALOG_ACTIVE = "ADD_LINKS_DIALOG_ACTIVE";
```

From background.js lines 5-9 (WRONG - must fix):
```javascript
const STORAGE_KEYS = {
 CLICKNLOAD_ACTIVE: 'settings_clicknload_active',        // WRONG
 CONTEXT_MENU_SIMPLE: 'settings_context_menu_simple',     // WRONG
 DEFAULT_PREFERRED_JD: 'settings_default_preferred_jd'    // WRONG
};
```

From background.js line 821 (WRONG - must fix):
```javascript
const settings = await chrome.storage.local.get(['settings_add_links_dialog_active', 'settings_default_preferred_jd']);
const shouldOpenPopup = settings.settings_add_links_dialog_active !== false;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix background.js storage keys and delete options page</name>
  <files>background.js, options.html, options.js</files>
  <action>
1. Fix background.js STORAGE_KEYS (lines 5-9) to match StorageService key strings:
   - CLICKNLOAD_ACTIVE: 'CLICKNLOAD_ACTIVE' (was 'settings_clicknload_active')
   - CONTEXT_MENU_SIMPLE: 'CONTEXT_MENU_SIMPLE' (was 'settings_context_menu_simple')
   - DEFAULT_PREFERRED_JD: 'DEFAULT_PREFERRED_JD' (was 'settings_default_preferred_jd')
   Add a comment: "// Key strings must match StorageService.settingsKeys values"

2. Fix background.js line 821 hardcoded keys:
   - Change `chrome.storage.local.get(['settings_add_links_dialog_active', 'settings_default_preferred_jd'])`
     to `chrome.storage.local.get(['ADD_LINKS_DIALOG_ACTIVE', 'DEFAULT_PREFERRED_JD'])`
   - Change `settings.settings_add_links_dialog_active` to `settings.ADD_LINKS_DIALOG_ACTIVE` (line 822)

3. Delete options.html and options.js (removes wrong APP_KEY myjd_webextension_mv3 and duplicate storage key mapping). Use git rm.

4. Verify no remaining occurrences of wrong APP_KEY: grep entire codebase for `myjd_webextension_mv3` — should return zero results after deletion.

5. Verify no remaining lowercase settings_ prefixed keys: grep codebase for `settings_clicknload_active`, `settings_context_menu_simple`, `settings_default_preferred_jd`, `settings_add_links_dialog_active` — all should be zero results.
  </action>
  <verify>
    <automated>npx jest --no-coverage -x 2>&1 | tail -5</automated>
  </verify>
  <done>background.js STORAGE_KEYS use uppercase key strings matching StorageService; line 821 uses correct keys; options.html and options.js deleted; no myjd_webextension_mv3 in codebase</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tests for storage key audit and options deletion</name>
  <files>scripts/__tests__/options.test.js, scripts/__tests__/background.test.js</files>
  <behavior>
    - Test: options.html does not exist on disk (file deleted)
    - Test: options.js does not exist on disk (file deleted)
    - Test: background.js STORAGE_KEYS values are uppercase (match StorageService)
    - Test: background.js has no lowercase 'settings_' prefixed storage key strings
    - Test: background.js line with chrome.storage.local.get uses ADD_LINKS_DIALOG_ACTIVE not settings_add_links_dialog_active
  </behavior>
  <action>
1. Rewrite scripts/__tests__/options.test.js:
   - Remove all existing tests (they test options.js content which no longer exists)
   - Add tests verifying options.html and options.js do NOT exist (fs.existsSync returns false)
   - Describe block: "Options Page Removal (Phase 9)"

2. Add storage key consistency tests to scripts/__tests__/background.test.js:
   - Read background.js source via fs.readFileSync
   - Test: STORAGE_KEYS values match StorageService format (CLICKNLOAD_ACTIVE, CONTEXT_MENU_SIMPLE, DEFAULT_PREFERRED_JD as uppercase strings)
   - Test: No occurrence of 'settings_clicknload_active', 'settings_context_menu_simple', 'settings_default_preferred_jd', or 'settings_add_links_dialog_active' in background.js
   - Use the established source-level regex test pattern from this project
  </action>
  <verify>
    <automated>npx jest --testPathPattern="(options|background)" --no-coverage -x</automated>
  </verify>
  <done>options.test.js verifies files deleted; background.test.js verifies correct storage key strings; all tests pass</done>
</task>

</tasks>

<verification>
1. `npx jest --no-coverage -x` — full test suite passes
2. `ls options.html options.js 2>/dev/null` — no output (files deleted)
3. `grep -r "myjd_webextension_mv3" --include="*.js" --include="*.html" .` — no results
4. `grep -r "settings_clicknload_active\|settings_context_menu_simple\|settings_default_preferred_jd\|settings_add_links_dialog_active" --include="*.js" .` — no results (excluding test files that test for absence)
</verification>

<success_criteria>
- background.js STORAGE_KEYS use correct uppercase key strings matching StorageService
- background.js CNL handler (line 821) uses ADD_LINKS_DIALOG_ACTIVE and DEFAULT_PREFERRED_JD
- options.html and options.js are deleted
- No wrong APP_KEY (myjd_webextension_mv3) anywhere in codebase
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-audit/09-01-SUMMARY.md`
</output>
