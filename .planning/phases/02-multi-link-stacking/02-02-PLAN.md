---
phase: 02-multi-link-stacking
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [LINK-01, LINK-02, LINK-04, LINK-05, LINK-06]

must_haves:
  truths:
    - "Right-clicking 5 links on a page shows all 5 in toolbar sidebar"
    - "Adding a link while toolbar is open updates the list without reopening"
    - "Closing a tab clears its link queue"
    - "Same URL right-clicked twice appears only once"
    - "Queue survives service worker termination and is restored on wake"
  artifacts:
    - path: "background.js"
      provides: "Queue accumulation, dedup, persistence, tab cleanup (already implemented in Phase 1)"
      contains: "addLinkToRequestQueue"
    - path: "scripts/controllers/ToolbarController.js"
      provides: "Real-time update listener (already implemented)"
      contains: "link-info-update"
  key_links:
    - from: "background.js"
      to: "chrome.storage.session"
      via: "persistQueue writes queue on every mutation"
      pattern: "chrome\\.storage\\.session\\.set"
    - from: "background.js"
      to: "ToolbarController.js"
      via: "chrome.runtime.sendMessage link-info-update"
      pattern: "link-info-update"
    - from: "background.js"
      to: "chrome.tabs.onRemoved"
      via: "Tab close listener deletes queue for closed tab"
      pattern: "tabs\\.onRemoved"
---

<objective>
Verify all six LINK requirements work end-to-end by running existing automated tests (LINK-01, LINK-02, LINK-04, LINK-05, LINK-06 from Phase 1) and performing manual E2E verification in the browser. These requirements were already implemented in Phase 1 -- this plan confirms they work together with the new batch send from Plan 01.

Purpose: LINK-01, LINK-02, LINK-04, LINK-05, LINK-06 were implemented in Phase 1 but not formally verified as a cohesive multi-link stacking flow. This plan confirms the complete E2E experience.
Output: Verified multi-link stacking flow, all tests green
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-queue-persistence/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite to verify all LINK requirements have automated coverage</name>
  <files></files>
  <action>
Run the full Jest test suite to verify all existing tests pass after the Plan 01 batch send refactor. Specifically confirm:

1. `npx jest --verbose` -- all tests pass (should be 50+ tests across 4 suites now)
2. Verify these specific test groups exist and pass:
   - Queue accumulation: `npx jest scripts/__tests__/background.test.js -t "persistQueue writes" --verbose`
   - Message routing: `npx jest scripts/__tests__/background.test.js -t "link-info-update" --verbose`
   - Queue restore: `npx jest scripts/__tests__/background.test.js -t "restoreRequestQueue" --verbose`
   - Duplicate detection: `npx jest scripts/__tests__/background.test.js -t "Duplicate link" --verbose`
   - Tab close cleanup: `npx jest scripts/__tests__/background.test.js -t "tabs.onRemoved" --verbose`
   - Batch send: `npx jest scripts/__tests__/AddLinksController.test.js --verbose`

If any tests fail, diagnose and fix (the fix belongs in this plan since it's integration verification). Do NOT modify test expectations -- fix the source code if there's a regression.
  </action>
  <verify>
    <automated>npx jest --verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All test suites pass. Queue accumulation (LINK-01), real-time update routing (LINK-02), persistence (LINK-04), dedup (LINK-05), tab cleanup (LINK-06), and batch send (LINK-03) all verified by automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual E2E verification of complete multi-link stacking flow</name>
  <files></files>
  <action>
Present the user with the complete multi-link stacking verification checklist. All automated work is complete -- this checkpoint verifies the real browser experience.

What was built: Complete multi-link stacking flow including right-click link accumulation, real-time toolbar updates, batch send to JDownloader, queue persistence across SW restart, duplicate detection, and tab close cleanup. Plan 01 added single-call batch sending. Phase 1 implemented all other LINK requirements.
  </action>
  <verify>
User performs 6 manual checks in browser:

1. **LINK-01 (Accumulation):** Open any page with multiple links. Right-click a link -> "Download with JDownloader". Toolbar opens with 1 link. Right-click a second link. Right-click a third link. Verify toolbar shows all 3 links.

2. **LINK-02 (Real-time update):** With toolbar still open from step 1, right-click a 4th link on the same page. Verify the toolbar list updates immediately (new link appears at top, no need to close/reopen toolbar).

3. **LINK-05 (Dedup):** Right-click the same link you already added. Verify it does NOT appear twice in the toolbar list.

4. **LINK-03 (Batch send):** With 3+ links queued, click "Add links" in the toolbar. Verify all links appear in JDownloader's Link Grabber in a single batch (they should all arrive together, not one at a time). If JDownloader is offline, verify error message appears and links remain in toolbar for retry.

5. **LINK-04 (Persistence):** Queue 2+ links in the toolbar. Go to chrome://extensions, find the extension, click "Service Worker" link to open DevTools, then click "Stop" in the SW inspector to terminate it. Now right-click another link on the page. Verify the previous links are still in the queue alongside the new one.

6. **LINK-06 (Tab cleanup):** Open a new tab, right-click a link to queue it. Close that tab. Open a new tab to the same page, right-click a link. Verify only the new link appears (old tab's queue was cleared).

Resume signal: Type "approved" if all 6 checks pass, or describe which check(s) failed with details.
  </verify>
  <done>User confirms all 6 LINK requirements work correctly in the browser. Phase 2 multi-link stacking is complete.</done>
</task>

</tasks>

<verification>
- `npx jest --verbose` -- all tests pass across all suites
- Manual E2E verification of all 6 LINK requirements in browser
</verification>

<success_criteria>
- All automated tests pass (50+ tests, 4 suites, zero failures)
- Manual verification confirms: link accumulation, real-time update, batch send, persistence, dedup, tab cleanup all work in browser
- User approves E2E flow via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-link-stacking/02-02-SUMMARY.md`
</output>
